export interface Cliente {
    nome: string;
    endereco: string;
    casado: boolean;
    dataCad: Date;
    dataMod: Date;
}


//interface vai ser usada quando precisar salvar os registros no banco